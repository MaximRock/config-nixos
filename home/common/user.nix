# home/common/user.nix
{ pkgs, lib, username, ... }:
{
  home.sessionVariables = {
    # EDITOR = "emacs";
  };

  # Пример: персональные настройки, которые могут зависеть от пользователя
  # (пока пусто, но структура готова)
}